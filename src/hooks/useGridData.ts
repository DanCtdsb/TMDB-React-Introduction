// import { GridProps } from "@/components/ImageGrid";
// import type { tvType } from "@/core/types";



// export const useGridData = (data: tvType) => {
//     const gridDataResults = data.results.map((item) => ({
//       id: item.id,
//       imagePath: item.poster_path,
//       primaryText: item.original_name
//   }));
//   return gridDataResults;
// }